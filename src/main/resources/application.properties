# spring.application.name=demo
# # Don't change the port
# server.port = 9001
# # for https
# server.forward-headers-strategy=framework 

# #Database Connectivity

# spring.datasource.url=jdbc:mysql://localhost:3306/sample?createDatabaseIfNotExist=true
# spring.datasource.username=root
# spring.datasource.password=Amypo
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# #Hibernate connectivity 
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.formate_sql=true
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect







# Application
spring.application.name=demo
server.port=9001
server.forward-headers-strategy=framework

# Database Connectivity
spring.datasource.url=jdbc:mysql://localhost:3306/sample?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Disable Open-in-View to avoid lazy-loading issues
spring.jpa.open-in-view=false
